import { FC } from "react";

const Header: FC = () => {
  return (
    <header className="w-screen h-[200px] bg-lightBlack mb-4">
      <div className="w-[70%] h-full m-auto"></div>
    </header>
  );
};

export default Header;
